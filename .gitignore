node_modules
dist

# ENV-Variablen (Supabase-Keys) niemals committen, siehe CLAUDE.md §6
.env
.env.*
*.local

# Usertest-Rohdaten (Antworten der Testpersonen) bleiben lokal, nicht oeffentlich
usertest/

.DS_Store
